from setuptools import setup, find_packages

setup(
    name="engine",
    version="0.1",
    packages=find_packages(),
    install_requires=[''],
    entry_points={
        
    },
    zip_safe=False
)
